[package]
name = "lobby"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
bodhi_config = { path = "../../../../crates/bodhi_config" }
serde = { version = "1", features = ["derive"] }
