[package]
name = "bodhi"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]

# Reuse workspace-managed serde and serde_json so crates can deserialize remote errors.
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
